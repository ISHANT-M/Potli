{
  "name": "potli",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Local dev tooling for the Potli frontend, backend, and database",
  "scripts": {
    "setup": "node scripts/setup.ts",
    "dev": "node scripts/dev.ts",
    "db:setup": "node scripts/setup.ts --only db --with-db",
    "build": "npm --prefix frontend run build",
    "typecheck": "tsc --noEmit && npm --prefix frontend run typecheck && npm --prefix backend run typecheck && npm --prefix db run typecheck"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.9.3"
  }
}
