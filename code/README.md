# Potli application

- `frontend/` — TypeScript, HTML5, Tailwind CSS, and Vite
- `backend/` — reserved for backend services and Supabase integration
- `db/` — reserved for Supabase migrations and seed data

## Run locally

```bash
cd frontend
npm install
npm run dev
```

Create a production build with `npm run build`.
